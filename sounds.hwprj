# Sounds project file

#makemode=on
#objpath=obj

$PORTAUDIO_DIR=..\Portaudio
$SNDFILE_DIR=..\libsndfile

{win}guilib=uni

libs=hbzlib hbmzip minizip portaudio
{win}libs=hbwin
{unix}libs=rt

{win}cflags=-I$PORTAUDIO_DIR;$SNDFILE_DIR\include

outname=sounds

srcpath=source
*.prg
*.c

srcpath=$HWGUIPATH/utils/editor/
hcediext.prg

{unix}def_lflags=/lib/x86_64-linux-gnu/libsndfile.so.1 `pkg-config --libs gtk+-2.0`